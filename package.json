{
  "name": "netlify-retry-function",
  "version": "1.0.0",
  "description": "A serverless application to monitor BoH frontend application deployments on Netlify and redeploy and notify the team for failing builds at intervals",
  "scripts": {
    "start": "npx functions-framework --target=deploymentMonitor",
    "run:function": "nodemon --exec \"npm run start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.5",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.2.0"
  }
}
